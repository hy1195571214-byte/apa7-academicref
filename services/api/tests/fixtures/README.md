# Test fixtures

Drop small sample files here when you want to add PDF / DOCX / image fixtures
for integration tests. DOCX fixtures are generated on the fly by
`test_ingest.py`, so nothing needs to live on disk for the unit tests to pass.
